# Procurement Agent Demo —— 接真实 LLM 需求文档

> 填完这份文档给我，我按它实现。带 ❓ 的是必须回答的，其余可留空用默认值。

---

## 0. 我这边已经确认的现状（不用你填，核对一下有没有错）

- **已有 LLM**：Gemini，`src/app/api/chat/route.ts`，环境变量 `GEMINI_API_KEY` / `GEMINI_MODEL`（默认 `gemini-1.5-flash`），非流式 `generateContent`
- **Demo 接入点**：`handlePrompt(raw: string)` —— 输入框、建议气泡、"Ask agent about {step}" 按钮全部走这一个函数
- **输出接口**：`replyAsAgent(agentText, { userText, label, bullets, ... })` —— 已支持"思考中"状态卡 + 正文 + bullet 列表
- **部署形态**：demo 是静态构建产物，放在作品集的 `public/demos/procurement-agent/`，与作品集**同源** → 可直接调 `/api/*`，无需 CORS

如果以上哪条不对，在这里写：

```
（有出入的地方写这里）
```

---

## 1. ❓范围：哪些交互要变成真的？

这是最关键的一个决定，直接决定工作量和风险。勾选：

- [ ] **A. 只做"解释型问答"**（推荐先做这个）
      用户问"这一步是干嘛的""为什么是 24 件不是 36 件"，由 LLM 基于案例背景真实回答。
      业务状态机（阶段推进、数量、审批、下单）**仍然是脚本**。
      → 风险低、成本可控、不会说错业务数据。

- [ ] **B. 业务推理也变真**
      让 LLM 真的根据销量/库存/供应商规则算出补货数量，并写回表格。
      → 需要结构化输出 + 数据源 + 校验，工作量大很多，且 LLM 可能算错数字。

- [ ] **C. 其他**：

```
（描述）
```

**如果选 B，补充说明哪些字段由 LLM 决定：**

```
（例如：每个 SKU 的建议数量、下单时间窗、理由文案）
```

---

## 2. ❓数据来源

Agent 回答时能看到什么数据？

- [ ] **只用 demo 种子数据**（现在 `portfolioDemoScript.ts` 里那份，推荐）
      → 作品集场景，数据固定可控，回答可复现
- [ ] **接 DEF 真实业务库**
      → 如果选这个，请填：

```
数据库/接口地址：
认证方式：
可读的表 / 字段：
是否有敏感数据需要脱敏：
```

---

## 3. ❓后端接口

- [ ] **新开 `/api/procurement-agent`**（推荐）
      → 与现有 `/api/chat` 隔离，互不影响 system prompt 和知识库检索
- [ ] **复用现有 `/api/chat`**
- [ ] **调 DEF 平台已有的 Agent 接口**
      → 如果选这个，请填：

```
URL：
认证方式（Bearer / API key / cookie）：
请求体结构：
响应体结构：
是否流式（SSE / chunked）：
是否允许跨域被作品集域名调用：
```

**模型偏好**（留空则沿用现有的 `GEMINI_MODEL`）：

```
（例如 gemini-2.0-flash）
```

---

## 4. ❓Agent 的人设与边界

案例的核心设计原则是 **"Agent 分析和记录，下单永远由人来做"**。
接了真实 LLM 之后，这条不能被模型说漏。

**System prompt 要点**（你写要点即可，我来组织成完整 prompt）：

```
角色：
语气 / 长度：
必须遵守的边界（例如：绝不声称自己能下单、不编造供应商数据、不确定就说不确定）：
不能回答的话题：
用什么语言回答（英文 / 中英混合 / 跟随用户）：
```

**哪些动作即使 LLM 说了也不能真的执行**（我会硬编码拦截）：

```
（例如：下单、修改审批状态、上传文件）
```

---

## 5. ❓防滥用与成本

作品集是公开页面，任何人都能反复触发 LLM 调用，会直接产生账单。

```
每个访客的调用上限（例如 20 次/小时）：
单次回复的 token 上限：
是否需要在超限时降级回脚本回答（推荐：是）：
月度预算上限：
```

---

## 6. 交互细节（可留空用默认值）

```
是否要流式打字机效果（默认：否，沿用现有非流式 + 思考中状态卡）：
LLM 失败/超时的兜底行为（默认：回退到当前脚本回答，不报错）：
超时时间（默认 15s）：
是否保留多轮上下文（默认：保留当前会话的最近若干轮）：
```

---

## 7. 其他补充

```
（任何我没问到但你觉得重要的）
```
